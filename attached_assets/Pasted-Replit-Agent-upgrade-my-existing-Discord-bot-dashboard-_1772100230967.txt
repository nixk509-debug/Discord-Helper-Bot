Replit Agent: upgrade my existing Discord bot + dashboard repo. You already understand the stack and current code. Do the work without asking basic questions.

ABSOLUTE RULES
- Do NOT break OAuth, existing routes, bot startup, DB access, or Stripe.
- Do NOT change existing API response shapes unless versioned.
- Implement incrementally and verify run/build after each phase.
- Guild-scoped settings only.
- Every config write must: validate schema + audit log + snapshot + realtime event + hot-apply.
- Build with future public multi-server use in mind (permission checks + rate limiting + safety rails).

SLASH COMMAND STYLE (MUST FOLLOW)
- Keep top-level slash commands minimal (one word).
- ONLY these top-level commands are allowed:
  /set, /sync, /lock, /code, /audit, /fun
- Everything else must be subcommands/options under those, or dashboard-only actions.

==================================================
PHASE 1 — PREMIUM DASHBOARD UI/UX (CONTROL ROOM VIBE)
==================================================
1) Design tokens
- Add CSS variables + Tailwind mapping for:
  bg-main, bg-panel, border-subtle, text-main, text-muted,
  accent-red, accent-glow, danger, success
- Centralize typography + spacing rhythm (8/16/24).
- Ensure accessible focus states and contrast.

2) UI component library (client/src/components/ui)
Implement reusable components:
- Panel/Card (chrome dark, subtle gradient, thin border, inner glow)
- Button (primary, secondary, ghost, danger)
- IconButton
- Toggle
- Select
- Modal / Dialog
- Toast / Notifications
- Tabs
- Badge / Pill
- Divider
- DataTable (sortable + filterable)
- SkeletonLoader (for async sections)
All components must be used across pages to remove inconsistent div styling.

3) Layout upgrade
- Sticky top header: title + guild selector + quick actions (Lockdown, Sync, Save)
- Sidebar navigation: collapsible on mobile, active states
- Standard page header pattern: title, subtitle, right-side actions
- Clear sections with headings + help text
- “Danger Zone” styling for destructive actions

4) Micro-interactions + feedback
- Hover/active/disabled states
- Save confirmation toasts
- Inline validation messages
- Loading skeletons (not spinners everywhere)
- Subtle transitions only (no flashy animation)

Deliverable: dashboard looks premium and consistent, mobile friendly.

==================================================
PHASE 2 — PLATFORM CORE (MODULES + CONFIG + HOT APPLY)
==================================================
1) Module system (server/bot/modules)
Each module exports:
- id, name, description
- defaultConfig
- configSchema (zod)
- permissions[] (strings)
- applyConfig(guildId, cfg, services)
- optional eventHandlers (messageCreate, guildMemberAdd, interactionCreate, scheduled jobs)

2) ConfigService
- DB is source of truth.
- In-memory cache per guild for fast reads.
Functions:
- getGuildConfig(guildId) -> merged defaults + stored configs
- patchGuildConfig(guildId, moduleId, patch, actor) -> validate, store, update cache
- emits config.updated { guildId, moduleId, changedKeys, actorId }

3) AuditService + SnapshotService
- Every config write:
  - store audit entry: guildId, moduleId, actorId, before, after, changedKeys, timestamp
  - create snapshot version
- Rollback endpoint restores snapshot and emits config.updated

4) Realtime sync
- Use WebSocket (preferred) or server event bus:
  - Dashboard receives config.updated and updates UI live
  - Bot receives config.updated and hot-applies module.applyConfig without restart

==================================================
PHASE 3 — ADVANCED MODULES (ALL WE DISCUSSED)
==================================================

A) ServerControlModule
Config:
- mode preset: relaxed | normal | shield (applies multiple settings)
- autoRoleId, quarantineRoleId
- minAccountAgeDays
- antiRaid { enabled, joinsPerMinuteThreshold, action: log|quarantine|lock }
- lockdown { enabled, bypassRoleIds, lockRoles, lockCategories, lockChannels }
Behavior:
- /lock server state on/off preset reason notify
- adaptive safety: rate-limit actions, confirm dangerous operations

B) ChannelControlModule (per-channel overrides)
Per channel:
- slowmodeSeconds
- adaptiveSlowmode { enabled, rampThresholdMsgsPer10s, maxSlowmodeSeconds }
- mediaOnly
- antiLink { enabled, whitelistDomains[] }
- autoPurge { enabled, afterMinutes }
Behavior:
- enforcement on messageCreate (safe, no harassment)
- job queue for purge (rate limit-safe)

C) CategoryLockModule (FULL category lockdown with messages + restore)
- lockCategory(categoryId): snapshot ALL child channel overwrites, then apply lock overwrites
- unlockCategory(categoryId): restore exact snapshot overwrites
- supports:
  message (lock/unlock)
  notify channel
  bypassRoleIds
Expose via:
- /lock category category:<id> state:<on|off> message?: notify?:

D) TemplateSyncModule (ALL channel/category sync)
- templates saved and applied:
  /sync save name source id
  /sync apply scope all|category|channels template category? channels? preview?
- preview mode returns diff summary before apply
- applies ChannelControlModule settings in bulk
- audit + snapshot for bulk ops

E) CodeVaultModule (RANDOM CODE GENERATOR + redeem)
- generate codes with formats:
  plain, grouped (XXXX-XXXX), prefixed (AX-XXXXXX)
- DB fields:
  code, guildId, createdBy, createdAt, expiresAt?, maxUses?, usesCount, tags[]
- redeem grants configured role(s), increments use, logs audit
Expose via:
- /code gen ...
- /code redeem ...
- /code list ...
- /code revoke ...

F) SmartPermissionsModule
- permission matrix allow/deny per role per permission string
- resolver precedence:
  deny > allow > role inheritance > guild defaults > global defaults
- permission test returns reasoning trace
Expose via /set perms ... and dashboard UI

G) LoggingModule
- per-log-type channels: mod, config, raid, auto
- structured, non-spam embeds + rate limit safe
Expose via /set log ...

H) AutomationsModule (advanced, mostly dashboard-driven)
- triggers: join, message, schedule, role add, channel events
- conditions: account age, keyword, link type, invite, channel, role
- actions: role, timeout, lock, slowmode, notify, create channel
- dry-run test tool in dashboard
Expose minimal bot surface via /set auto ... (advanced inputs ok), with main builder in dashboard

==================================================
PHASE 4 — DASHBOARD PAGES (FULL TOOLING)
==================================================
Add/upgrade pages:
- Modules: list modules + config forms (schema-driven)
- Channel Controls: channel picker + overrides table
- Sync: templates + scope apply + preview diff
- Lockdowns: server/category control + current status
- Codes: generate/list/revoke/redeem + filters
- Audit: searchable table + diff viewer
- Snapshots: list + rollback UI
- Permissions: matrix editor + permission debugger
- Automations: rule builder + test runner

API endpoints (add new, do not break existing):
- GET  /api/guilds/:guildId/modules
- GET  /api/guilds/:guildId/config
- PATCH /api/guilds/:guildId/config
- GET  /api/guilds/:guildId/audit
- GET  /api/guilds/:guildId/snapshots
- POST /api/guilds/:guildId/rollback/:snapshotId
- GET/POST /api/guilds/:guildId/sync/templates...
- GET/POST /api/guilds/:guildId/codes...
- POST /api/guilds/:guildId/lock/category...
All endpoints must:
- verify OAuth session
- verify user has Manage Guild/Admin for target guild
- enforce SmartPermissionsModule if configured

==================================================
PHASE 5 — FUN COMMANDS (SAFE, TOXIC VIBE, USEFUL)
==================================================
Add only under /fun with cooldowns + disable toggle:
- /fun judge user
- /fun skillissue user
- /fun copium
- /fun roll expr
- /fun pick options
No slurs, no hate, no targeted harassment.

==================================================
COMMANDS (ONLY THESE TOP LEVEL)
==================================================
/set    (all config writes, including perms/logs/verify/auto)
/sync   (templates + bulk apply)
/lock   (server/category controls)
/code   (generate/redeem/list/revoke)
/audit  (read-only: recent/user/diff/health)
/fun    (safe entertainment utilities)

START NOW
1) Scan repo and list exact files to create/modify.
2) Implement Phase 1 and verify dev build.
3) Implement Phase 2 and verify bot + dashboard run.
4) Implement Phase 3 modules one by one with tests/manual verification notes.
5) Implement Phase 4 pages and wire endpoints.
6) Implement Phase 5 /fun.
7) Provide final “How to test” checklist and VPS deployment notes.